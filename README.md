# rust-telegram
Rust telegram bot (should be faster than go)

### todo
[x] client (used mpsc for handling updates)
[x] the model system, which wrap default structs, making possible to have inner methods
[ ] figure out how to convert seamless structs to models
[ ] db (pg/redis)
[ ] sth cool
[ ] tests (i don't promise)
